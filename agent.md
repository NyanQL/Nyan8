# Nyan8 作業方針・確認項目

## ファイル構成の必須方針

- 本体のGo実装は **`main.go` の1ファイル**に集約する。別の `.go` ファイルやパッケージへ分割しない。
- Goテストとテスト用の補助関数・型は **`main_test.go` の1ファイル**に集約する。別の `*_test.go` を作成しない。
- 機能追加、修正、リファクタリングでもこの方針を維持する。ファイルが長くなっても、整理は各ファイル内で行う。
- 利用者から明示的な方針変更がない限り、ファイル分割を行わない。

## NyanPUIとの比較・調整の目的

- 相互に置き換えられることや、同じJavaScript・設定をそのまま使えることは目的ではない。
- 同名関数は、各製品内で同じような意味の結果を返すことを目指す。
- 処理対象、戻り値の意味、失敗の伝え方、チェックやPushなどの仕組みの挙動を確認する。
- HTMLとJSONという用途の違い、専用機能の有無、単なる実装方法の違いだけでは修正対象としない。
- NyanPUI側を一律に正解とはしない。必要に応じてどちらの仕様を採るか確認する。

## 進捗の記録方法

- `docs/` 以下の資料はローカル専用とし、Gitの追跡・コミット対象にしない。強制追加もしない。
- `docks/` は誤記のため使用しない。資料は `docs/` に保存し、`docks/` もGitの対象外とする。
- 以下の番号は固定する。完了しても削除・詰め直し・再採番しない。
- 着手時と完了時には、対象の番号と項目名を利用者に示す。
- 完了した項目は、番号を残して状態・変更内容・検証結果を更新する。
- この一覧は確認・対応候補の記録であり、未決の仕様をすべて自動的に変更する指示ではない。

## 固定番号の確認・対応一覧

確認項目1～18の内容・状態と対応済みの作業は、次の文書に記録しています。進捗はこの文書を更新してください。

[20260922_204654_nyan8_nyanpui_checklist.md](docs/20260922_204654_nyan8_nyanpui_checklist.md)
