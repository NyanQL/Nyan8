# Nyan8・NyanPUI 確認・対応一覧

作成日時：2026-09-22 20:46:54 JST

同名関数の結果と、チェック・Pushなどの仕組みの挙動を確認するための一覧です。コードや設定の相互置換は目的としません。

項目番号は固定し、完了しても削除・再採番しません。着手・完了時は番号を明示し、この文書の状態・変更内容・検証結果を更新します。実装方針は [agent.md](../agent.md) を参照してください。

## 固定番号の確認・対応一覧

| 番号 | 項目 | 確認・対応内容 | 状態 |
|---|---|---|---|
| 1 | ファイルの相対パス | `nyanGetFile`、`nyanReadFileB64`、添付ファイル関連の相対パスは、多段include先の定義JSONではなく、最上位の `api.json` のディレクトリを基準にする。 | 対応済み（2026-09-22） |
| 2 | `nyanCallMe()` のAPI名省略 | Nyan8では `api` を必須にし、省略時はJavaScript例外とする。通常HTTPでは未捕捉の例外を500エラーとして返す。 | Nyan8対応済み（2026-09-23） |
| 3 | `nyanCallMe()` のチェック拒否 | `paramCheck`・`outCheck` の拒否はチェック結果として返す。`checkOnly` も同様。Nyan8の現行仕様を維持する。 | 方針確定・Nyan8修正不要（2026-09-23） |
| 4 | `nyanHostExec()` の失敗 | 終了コード0は成功結果、0以外も終了コード・標準出力・標準エラーを含む失敗結果を返す。引数未指定・シェル起動失敗などは例外とする。 | Nyan8対応済み（2026-09-23） |
| 5 | `nyanGetAPI()` の通信失敗 | 通信失敗はJavaScript例外とするNyan8の現行仕様を維持する。NyanPUIの修正は行わない。 | 方針確定・修正不要（2026-09-23） |
| 6 | `nyanGetItem()` の未登録キー | Nyan8は空文字列、PUIは `null` を返す。未登録と空の保存値を区別するか決める。 | 未対応・方針確認 |
| 7 | `nyanSetCookie()` のHTTPS対応 | Nyan8がHTTPSで受信した場合に `Secure` 属性を付ける。HTTP受信時には付けない。 | Nyan8対応済み（2026-09-23） |
| 8 | `nyanBase64Decode()` の不正入力 | Nyan8のOAuth用関数は、不正入力時に空文字列を返す現行仕様を維持する。 | 方針確定・修正不要（2026-09-23） |
| 9 | `nyanRandomBase64URL()` | NyanPUIに合わせ、引数省略時は32バイト、指定可能な範囲は1～1024バイトとする。 | Nyan8対応済み（2026-09-23） |
| 10 | ルートHTTP `/?api=...` のチェック | 呼び出し先の `paramCheck`・`outCheck`・`checkOnly` を適用する。拒否・`checkOnly` 時はPushを実行しない。 | Nyan8対応済み（2026-09-23） |
| 11 | MCPのチェック | HTTP・stdioともにToolの参照先APIの `paramCheck`・`outCheck` を適用する。`checkOnly` は `paramCheck` の結果だけを返し、本体・`outCheck` を実行しない。 | Nyan8対応済み（2026-09-23） |
| 12 | OAuthのチェック | OAuth用API・Go生成メタデータ・内部トークン検証に `paramCheck`・`outCheck` を適用する。HTTPの `checkOnly` は入力チェックだけで終了する。 | Nyan8対応済み（2026-09-23） |
| 13 | PUIのWebSocket `checkOnly` | チェックが通過するかどうかが分かればよく、成功時の独自 `result` が `null` になる挙動は許容する。両製品の現行仕様を維持する。 | 方針確定・修正不要（2026-09-23） |
| 14 | Pushの複数接続 | 同じAPIの全購読接続へ同じ結果を配信する。Push先のチェック・本体は各1回だけ実行する。 | Nyan8対応済み（2026-09-23） |
| 15 | Push接続の解除 | 終了・送信失敗した接続だけを登録から外し、ほかの接続と新しい接続の登録を維持する。 | Nyan8対応済み（2026-09-23） |
| 16 | Pushの実行条件 | 呼び出し元の `status` が200～399かつ `success` が真偽値の `false` ではない場合だけPushする。エラー結果ではPush先を実行しない。 | Nyan8対応済み（2026-09-23） |
| 17 | 入力パラメータの優先順位 | 通常HTTPでは本文の値を優先するNyan8の現行仕様を維持する。NyanPUI側は変更しない。 | 方針確定・Nyan8修正不要（2026-09-23） |
| 18 | リクエスト情報の引き継ぎ | WebSocketは接続時、Pushは呼び出し元、HTTP MCPはそのリクエストのCookie・IP・User-Agent・ヘッダーを、チェックと本体から取得可能にする。 | Nyan8対応済み（2026-09-23） |

## 対応済みの作業

- **項目18・リクエスト情報の引き継ぎ（2026-09-23）**：WebSocket接続後のメッセージ処理、HTTP・ルートHTTP・JSON-RPC・WebSocket起点のPush、HTTP MCPの `paramCheck`・本体・`outCheck` で、`nyanGetCookie`・`nyanGetRemoteIP`・`nyanGetUserAgent`・`nyanGetRequestHeaders` が実際のリクエスト情報を返すよう変更した。WebSocketは接続時、Pushは呼び出し元、HTTP MCPはTool呼び出しのリクエストを使用する。元のHTTPリクエストをコピーし、応答Writerを持たない読み取り専用の実行コンテキストとして渡す。`nyanCallMe` による内部呼び出しにも引き継ぐ。これらの経路の `nyanSetCookie` は応答を変更しない。stdioとリクエストのない実行では、取得関数は従来どおり空文字列／空オブジェクトを返す。入力パラメータや受信者のCookieを取得元にせず、転送ヘッダーによるIP上書きも行わない。`main_test.go` で各経路の前後チェック・本体・内部呼び出し、送信元と複数受信者の情報の分離、偽装パラメータの非採用、リクエストコピーの保持、Cookie設定の無効化、HTTP MCPとstdioの通常実行・`checkOnly` を検証した。`go test ./... -count=1` と関連する `go test -race` が通過し、READMEを更新した。NyanPUI側は変更していない。
- **項目17・入力パラメータの優先順位（2026-09-23）**：利用者の判断により、URLクエリとJSON・フォーム本文の同名項目は本文を優先する方針に確定した。Nyan8の通常HTTP（通常パス・ルート経由）は既にこの動作のため、実装変更は不要。`nyan_mode` にも同じ優先順位を適用する。NyanPUIを修正しない既存方針を維持し、クエリ優先との差は許容する。READMEと確認一覧を更新した。コード変更・テストの再実行は行っていない。
- **項目16・エラー結果でのPush抑止（2026-09-23）**：利用者の意向に従い、HTTP・ルートHTTP・WebSocket・JSON-RPCで呼び出し元の結果による共通のPush判定を追加した。`status` が200～399で、トップレベルの `success` が真偽値の `false` ではない場合だけPush先へ進む。400・409・500・503などのエラー結果や `success: false` では、Push先のチェック・本体・配信を実行しない。呼び出し元への応答内容・形式は維持し、HTTP・WebSocketではエラー結果の出力チェックも維持した。JSON-RPCの `success: false` は従来どおりエラー応答で終了し、出力チェック・Pushへ進まない。WebSocket・JSON-RPCのステータス省略とWebSocketのプレーンテキストは既存の既定値200を使う。`main_test.go` で4経路の正常・エラー結果、`success` と `status` の組み合わせ、省略・プレーンテキスト、例外、元の応答の保持、Push先の実行抑止と3接続への配信有無を検証した。`go test ./... -count=1` が通過し、READMEを更新した。NyanPUI側は変更していない。
- **項目14・Pushの複数接続／項目15・接続の解除（2026-09-23）**：利用者の意向に従い、APIごとに接続一覧を保持し、HTTP・ルートHTTP・JSON-RPC・WebSocket起点のPushを登録済みの全接続へ配信するよう変更した。Push先の入力チェック・本体・出力チェックは各1回だけ実行し、同じ結果を配信する。配信開始時に接続一覧をコピーし、登録一覧のロックを保持せずに送信する。接続ごとの書き込み直列化は維持した。切断時はその接続だけを除去し、最後の接続が終了した場合にAPIの登録を削除する。送信失敗時は該当接続だけを閉じて除去し、残りへの配信を続ける。`main_test.go` で各経路から3接続への配信、別APIへの非配信、チェック・本体の実行回数、拒否時の配信抑止、古い接続・途中の接続の終了後も残りと再接続先へ届くこと、最後の接続の削除、送信失敗後の配信継続、並行する登録・削除を検証した。`go test ./... -count=1` とPush・WebSocket関連の `go test -race` が通過。READMEを更新した。NyanPUI側は変更していない。呼び出し元APIのHTTPエラー時のPush条件（項目16）は変更していない。
- **項目13・PUIのWebSocket `checkOnly`（2026-09-23）**：利用者の判断により、確認したいのはチェックが通過するかどうかであり、成功時の独自 `result` が失われても問題なしとした。NyanPUIが成功時に `result: null` を返す挙動と、Nyan8がチェック結果の `result` を保持する挙動の差を許容する。両製品とも実装変更は不要。今回は確認一覧のみ更新し、テストは再実行していない。
- **項目12・OAuthのチェック（2026-09-23）**：`authorize`・`token`・`register`・`adminUser`、Goが生成する2種類のメタデータ、内部の `verifyAccess` に前後チェックを追加した。チェックは本体と同じOAuth専用環境・入力情報を使用し、それぞれ新しいVMと15秒のタイムアウトで実行する。HTTPの拒否・`checkOnly` は `{success, status, result}` をチェックのHTTPステータスで返す。クエリ・フォーム・JSON本文の `nyan_mode` を受け付け、本文を優先する。`checkOnly` では本体・メタデータ生成・出力チェックを実行せず、入力チェック未設定なら成功200・result nullを返す。HTTPの出力チェックには送信予定の本文・ステータス・Content-Type・検証済みの許可ヘッダーを渡し、チェックが検査情報を書き換えても送信内容は変わらない。拒否時は本体のCookie・Locationを送信しない。内部トークン検証では判定全体を検査し、チェック拒否・エラーなら401認証失敗としてToolを実行しない。Toolの `checkOnly` は認証側へ伝播させない。メタデータにもOAuthのレート・同時実行制限を適用する。`main_test.go` で通常URLとルートURL、各OAuth処理・メタデータ、実行順序、拒否・例外・形式不正・ファイル欠落、入力優先順位、チェック応答・本体のサイズ制限、ヘッダー制約、出力メタデータ、Toolの認証・実行抑止を検証した。`go test ./... -count=1` が通過（ローカル通信制限のため権限付きで再実行）。READMEを更新した。NyanPUI側は変更していない。
- **項目11・MCPのチェック（2026-09-23）**：HTTP・stdio共通のTool実行処理を、入力スキーマ検証 → `paramCheck` → 本体 → `outCheck` → 出力スキーマ検証の順に変更した。チェック拒否時は `{success, status, result}` をMCP結果の `content` と `structuredContent` に格納し、`isError: true` とする。`arguments.nyan_mode: "checkOnly"` では入力チェック結果だけを返し、本体・出力チェック・本体用出力スキーマ検証を実行しない。チェック未設定なら成功200・result nullを返す。成功した `checkOnly` は `isError: false`。入力スキーマ検証は維持するため、必要に応じてスキーマで `nyan_mode` を許可する。チェックの実行失敗・形式不正は詳細を含まないToolエラーとし、チェック結果にも既存の結果サイズ上限を適用する。`main_test.go` で両トランスポートの実行順序、チェック拒否・例外・不正結果・ファイル欠落、`checkOnly`、別名設定、スキーマ検証、サイズ制限、信頼済みprincipal、JSON出力メタデータ、設定再読込前のスナップショット保持を検証した。`go test ./... -count=1` が通過し、READMEを更新した。MCPのPush非実行とHTTPリクエストコンテキスト非引き継ぎ（項目18）は従来どおり。NyanPUI側は変更していない。
- **項目10・ルートHTTPのチェック（2026-09-23）**：`/?api=API名` の通常HTTP処理で、対象APIの `paramCheck` → 本体 → `outCheck` → Push → 応答の順に実行するよう変更した。入力チェック拒否時は本体・出力チェック・Pushを、出力チェック拒否時はPushを実行せず、チェック結果をHTTP応答として返す。`checkOnly` は入力チェックの結果だけを返し、チェック未設定なら `{success: true, status: 200, result: null}` を返す。既存のチェック処理・別名設定を共用する。`main_test.go` でクエリー・JSON・フォーム入力について通常パスとの結果一致、実行順序、拒否・例外・不正結果・チェックファイル欠落・本体エラー時の停止、`checkOnly`、ヘッダー・Cookieの取得、出力メタデータ、Push処理の抑止を検証した。`go test ./... -count=1` が通過（ローカル通信制限のため権限付きで再実行）。READMEを更新した。本体がHTTPエラーを返した場合のPush条件（項目16）は変更していない。
- **項目9・`nyanRandomBase64URL()` の既定値と範囲（2026-09-23）**：Nyan8のOAuth用関数をNyanPUIに合わせ、引数省略時は32バイト、指定範囲は1～1024バイトへ変更した。引数はエンコード前の乱数バイト数で、返却形式は従来どおりパディングなしBase64URL文字列。範囲外はJavaScript例外とする。明示的な `undefined`・`null` は引数省略と区別し、NyanPUIと同様に整数変換後の範囲チェックで拒否する。`main_test.go` で既定値、最小・最大値、従来範囲内外の有効値、範囲外の拒否、デコード後のバイト数、出力文字数と形式を検証した。`go test ./... -count=1` が通過し、READMEを更新した。NyanPUI側は変更していない。
- **項目8・`nyanBase64Decode()` の不正入力（2026-09-23）**：利用者の判断により、Nyan8のOAuth用 `nyanBase64Decode()` は不正なBase64入力時に空文字列を返す現行仕様を維持する。正常な空文字列のデコード結果も空文字列となり、両者を戻り値で区別しない。実装とREADMEはこの方針に一致しているため、確認一覧のみ更新した。コード変更・テストの再実行は行っていない。NyanPUI側も変更しない。
- **項目7・`nyanSetCookie()` のHTTPS対応（2026-09-23）**：受信リクエストのTLS情報に基づいて `Secure` 属性を設定するよう変更した。HTTPS受信時に付与し、HTTP受信時には付与しない。`X-Forwarded-Proto`・`Forwarded` は判定に使用しない。有効期間1時間・`Path=/`・`HttpOnly` と既存のHTTPコンテキストがない場合の挙動を維持した。`main_test.go` でHTTP・HTTPS、転送ヘッダーに影響されないこと、Cookieの他属性、リクエストやHTTPコンテキストがない場合を検証した。`go test ./... -count=1` が通過し、READMEを更新した。
- **項目5・`nyanGetAPI()` の通信失敗（2026-09-23）**：利用者の判断により、URL不正・通信失敗・応答本文の読み取り失敗をJavaScript例外として伝えるNyan8の現行仕様を維持する。HTTP 404・500などでも本文を取得できれば本文を返し、正常な空の本文は空文字列を返す。NyanPUIの修正は不要との指示により、PUI側も変更しない。両者の通信失敗時の差は残る。今回は確認一覧のみ更新し、コード変更・テストの再実行は行っていない。
- **項目4・`nyanHostExec()` の失敗結果（2026-09-23）**：「失敗を含む実行結果を返す」という利用者の意向を、終了コード0以外も結果を返す方針として適用した。コマンドの終了による失敗はJavaScript例外にせず、`success: false`・`exit_code`・`stdout`・`stderr` を返す。終了コード0は従来どおり `success: true` と結果を返す。返却形式はJavaScriptオブジェクトを維持した。引数未指定やシェル自体を起動できない場合などは例外を維持する。`main_test.go` で終了コード0・1・7と出力の保持、シェルが返すコマンド未発見の結果、引数未指定・シェル未発見時の例外を検証し、macOSで `go test ./... -count=1` が通過した。READMEを更新した。NyanPUI側は変更していない。
- **項目3・`nyanCallMe()` のチェック拒否（2026-09-23）**：利用者の判断により、`paramCheck`・`outCheck` が返す拒否結果は例外に変換せず、`success`・`status`・`result` を持つオブジェクトとして呼び出し元へ返す方針に確定した。`checkOnly` も成功・拒否ともにチェック結果を返す。チェック処理自体の実行エラーや戻り値の形式不正は、引き続きJavaScript例外とする。Nyan8の現行実装とREADMEはこの方針に一致しており、コード変更は不要。今回は記録のみを更新し、テストは再実行していない。NyanPUI側の例外を投げる実装との差は残っており、今回は変更していない。
- **項目2・`nyanCallMe()` のAPI名必須化（2026-09-23）**：Nyan8の固定呼び出し先 `hello2` を廃止し、引数オブジェクトの `api` に空白のみではない文字列を必須とした。未指定・空文字列・空白のみ・文字列以外の場合は `nyanCallMe: api is required (non-empty string)` のJavaScript例外を投げ、呼び出し先のチェック・本体を実行しない。通常HTTPでは未捕捉時に500エラーを返す。`try` / `catch` による捕捉は可能で、関数自体はHTTP応答を書き込まない。API名の明示指定による呼び出しは維持した。`main_test.go` で15種類の不正・省略入力、HTTP 500、暗黙の `hello2` 実行がないこと、例外捕捉、`hello2` の明示呼び出しを検証し、`go test ./... -count=1` が通過した。READMEを更新した。NyanPUI側は今回変更していない。
- **項目1・ファイルの相対パス（2026-09-22）**：`nyanGetFile`・`nyanReadFileB64`・`nyanSendMailAttachment`・`nyanSendMail` の添付 `path` は、実行時の設定スナップショットが保持する最上位の `api.json` のディレクトリを基準にするよう変更した。多段include先、本体・`paramCheck`・`outCheck`、`nyanCallMe`、Pushで共通の基準を使用する。絶対パスと未存在・ディレクトリ指定時の扱いを維持し、設定再読込後も開始済みの実行は元のスナップショットの基準を使用する。設定JSON内の `script`・チェック・include先などのパス解決は変更していない。`main_test.go` で子・孫のinclude、内部呼び出しと前後チェック、WebSocket経由のPush配信、ローカルSMTPでの添付内容、絶対パス、スナップショット保持を検証した。`go test ./... -count=1` が通過（ローカル通信制限のため権限付きで再実行）。READMEを更新した。
- **Push先のチェック**：HTTP・ルートHTTP・JSON-RPC・WebSocket起点のPushで、Push先の `paramCheck` → 本体 → `outCheck` → 配信を実行する。拒否・エラー時はPushを停止し、呼び出し元APIの応答は維持する。`checkOnly` ではPush先の本体・`outCheck`・配信を行わない。これは番号14～16の完了を意味しない。
- **WebSocketのチェック**：API指定の接続前に `paramCheck` を実行し、接続後は各メッセージの対象APIで `paramCheck` → 本体 → `outCheck` → 応答を実行する。接続時の `checkOnly` はアップグレードせずHTTPで結果を返す。API未指定のルート接続ではメッセージ受信時にチェックする。接続前には本体・`outCheck`・Pushを実行しない。
- **WebSocketの同時送信対策**：Pushと通常応答の書き込みを接続ごとに直列化した。
- **テストの集約**：6個のテストファイルを `main_test.go` に集約した。184個のテストエントリーポイントと補助処理を保持し、元のテスト本文を保持していることを確認した。

記録時点では、上記の実装について全テストと関連する競合検査が通過している。テスト集約後も `go test ./... -count=1` が通過した。テスト用のローカル通信がサンドボックスで拒否された場合は、必要な権限で再実行して検証する。
